# Source photos

Drop your **portrait** puzzle photo here.

Supported formats: `.jpg`, `.jpeg`, `.png`, `.webp`

Example:

```
shared/source/mushroom.jpg
```

Then run the chop script from the repo root (see [tools/chop_puzzle/README.md](../../tools/chop_puzzle/README.md)).
